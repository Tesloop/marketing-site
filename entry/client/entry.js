// Methods for optimistic updates
//import 'MarketingApp/todo-methods';

injectTapEventPlugin();

import './routes';

/*
Accounts.ui.config({
  passwordSignupFields: 'USERNAME_ONLY'
});
*/

if (process.env.NODE_ENV !== 'production') {
  // Load integration tests
}
